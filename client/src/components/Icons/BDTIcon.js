import React from 'react';

const BDTIcon = ({ className = "w-5 h-5" }) => {
    return (
        <span className={`inline-flex items-center justify-center font-bold ${className}`}>
            ৳
        </span>
    );
};

export default BDTIcon;
